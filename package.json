{
  "name": "restalize",
  "version": "1.0.0",
  "description": "Robust REST API generator. Generates a full typescript express-js REST API project from just a relational database connection",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:kennasoft/restalize.git"
  },
  "bin": {
    "restalize": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && cp -R templates dist",
    "prerelease": "rimraf ./dist/",
    "release": "tsc && cp -R templates dist",
    "prepublish": "yarn release",
    "start": "rimraf ./output && yarn build && npx ./dist/index.js output --template typeorm -d comicbooks_db -u root -x root"
  },
  "author": "Ikenna Okonkwo<ikenna.okonkwo@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/cross-spawn": "^6.0.1",
    "@types/prompts": "^2.0.8",
    "chalk": "^4.0.0",
    "commander": "^5.1.0",
    "cpy": "^8.1.0",
    "cross-spawn": "^7.0.2",
    "make-dir": "^3.1.0",
    "prompts": "^2.3.2",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
